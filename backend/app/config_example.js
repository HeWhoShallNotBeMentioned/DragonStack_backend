const SECOND = 1000;
const MINUTE = SECOND * 60;
const HOUR = MINUTE * 60;
const DAY = HOUR * 24;

const REFRESH_RATE = 0;

const STARTING_BALANCE = 0;

module.exports = {
  SECOND,
  MINUTE,
  HOUR,
  DAY,
  REFRESH_RATE,
  STARTING_BALANCE,
};
